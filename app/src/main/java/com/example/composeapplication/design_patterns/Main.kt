package com.example.composeapplication.design_patterns

import com.example.composeapplication.design_patterns.chain_of_responsibility.ChainOfResponsibilitySampleRunner
import com.example.composeapplication.design_patterns.decorator.DecoratorSampleRunner
import com.example.composeapplication.design_patterns.factory.FactorySampleRunner
import com.example.composeapplication.design_patterns.observer.ObserverSampleRunner
import com.example.composeapplication.design_patterns.singleton.SingletonSampleRunner

fun main() {

    //SingletonSampleRunner().runSample()
    //FactorySampleRunner().runSample()
    //DecoratorSampleRunner().runSample()
    //ChainOfResponsibilitySampleRunner().runSample()
    ObserverSampleRunner().runSample()
}